import React from 'react';
import './scrollMouse.css'; // Assuming you have this CSS in a separate file

const ScrollMouse = () => (
  <span className="scroll-btn">
      <span className="mouse">
        <span></span>
      </span>
  </span>
);

export default ScrollMouse;
